import { ValorMaskPipe } from './valor-mask-pipe';

describe('ValorMaskPipe', () => {
  it('create an instance', () => {
    const pipe = new ValorMaskPipe();
    expect(pipe).toBeTruthy();
  });
});
